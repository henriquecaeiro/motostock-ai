[pytest]
testpaths = tests
markers =
    unit: fast tests that exercise one module or service in isolation
    integration: tests that exercise multiple application components or SQLite
    e2e: complete API workflow tests using a temporary database
    manual: opt-in checks that require a running external Ollama service
